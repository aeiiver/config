### general

auto-reload yes
reload-threads 4

### keybinds

bind-key DOWN down
bind-key UP up
bind-key RIGHT open
bind-key LEFT quit
unbind-key j; unbind-key J
unbind-key k; unbind-key K
bind-key j down
bind-key k up
bind-key l open
bind-key h quit
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key G end
bind-key g home
bind-key d pagedown
bind-key u pageup
bind-key n next-unread
bind-key N prev-unread

### macros

macro o set browser "librewolf %u"
macro i set browser "feh %u"
macro m set browser "mpv %u"

### colorscheme (based on universal-color)

# see original code here:
# /usr/share/doc/newsboat/contrib/colorschemes/universal-color

# A simple color scheme designed to work well with pretty much any terminal:
#   Uses the basic 16 ANSI colors
#   Works with dark and light (inverted) terminal color schemes
#   Avoids color-on-color text
#   Avoids blue and yellow as those are often too bright or too dim
#   Works with Linux framebuffer terminal despite it being weird with bold text

color background         default default
color listnormal         default default
color listnormal_unread  green   default
color listfocus          default default bold
color listfocus_unread   green   default bold
color title              cyan    default bold
color info               magenta default bold
color hint-description   magenta default
color article            default default
color end-of-text-marker black default

# highlight urls
highlight article "[0-9A-Za-z._-]+[.][0-9A-Za-z._/#-]+" green default underline
highlight article "^[a-zA-Z]+://[0-9A-Za-z._-]+[.][0-9A-Za-z._/#-]+" green default underline

# make headings apparent
highlight all "---.+---"       yellow default bold
highlight feedlist ".+0/0.."   default default invis

# make articles colorful
highlight article "^Feed:.+"   yellow default
highlight article "^Title:.+"  red default
highlight article "^Author:.+" blue default
highlight article "^Date:.+"   magenta default
highlight article "^Link:.+"   green default
highlight article "^Links:"    yellow default bold
highlight article "\\[[0-9]+\\]"                  default default bold
highlight article "\\[image [0-9]+.+\\]"          cyan default
highlight article "\\[embedded flash [0-9]+.+\\]" yellow default
highlight article "\\[[0-9].+\\(link\\)$"           green default
highlight article "\\[[0-9].+\\(image\\)$"          cyan default
highlight article "\\[[0-9].+\\(embedded flash\\)$" yellow default
